jtype: Flow
with:
  name: nowapi
jcloud:
  version: ${{ ENV.JINA_VERSION }}
  custom_dns:
    - ${{ ENV.CUSTOM_DNS }}
  labels:
    team: now
gateway:
  protocol: [http]
  port: [8081]
  monitoring: true
  cors: true
  prefetch: 10
  env:
    JINA_LOG_LEVEL: DEBUG
