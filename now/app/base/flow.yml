jtype: Flow
with:
  name: nowapi
jcloud:
  version: ${{ ENV.JINA_VERSION }}
  custom_dns:
    - ${{ ENV.CUSTOM_DNS }}
  labels:
    team: now
gateway:
  protocol: [http, http, http]
  port: [8081, 8080, 8501]
  monitoring: true
  cors: true
  prefetch: 10
  env:
    JINA_LOG_LEVEL: DEBUG
