jtype: Flow
with:
  name: nowapi
  monitoring: true
  cors: true
  prefetch: 10
  env:
    JINA_LOG_LEVEL: DEBUG
jcloud:
  version: ${{ ENV.JINA_VERSION }}
  custom_dns:
    - ${{ ENV.CUSTOM_DNS }}
  labels:
    team: now
gateway:
  protocol: [grpc]
  port: [8080]